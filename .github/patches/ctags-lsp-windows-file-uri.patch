diff --git a/main.go b/main.go
index 0000000..0000000 100644
--- a/main.go
+++ b/main.go
@@ -741,7 +741,7 @@ func normalizeFileURI(uri string) (string, error) {
 		return "", fmt.Errorf("empty file URI")
 	}
 
-	path := filepath.Clean(filepath.FromSlash(parsed.Path))
+	path := fileURIPathToPath(parsed.Path)
 
 	absPath, err := filepath.Abs(path)
 	if err != nil {
@@ -755,7 +755,14 @@ func normalizeFileURI(uri string) (string, error) {
 // fileURIToPath expects normalized URIs.
 func fileURIToPath(uri string) string {
 	parsed, _ := url.Parse(uri)
-	return filepath.Clean(filepath.FromSlash(parsed.Path))
+	return fileURIPathToPath(parsed.Path)
+}
+
+func fileURIPathToPath(path string) string {
+	if len(path) >= 3 && path[0] == '/' && path[2] == ':' {
+		path = path[1:]
+	}
+	return filepath.Clean(filepath.FromSlash(path))
 }
 
 func resolveTagfilePath(rootURI, tagfilePath string) string {
